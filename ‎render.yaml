services:
  - type: web
    name: brief-docx
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn app:app --host 0.0.0.0 --port $PORT
    staticPublishPath: generated       # הופך /generated ל־/static
    plan: free
